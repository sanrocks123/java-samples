package com.sanjeev.sample;

import org.testng.Assert;
import org.testng.annotations.Test;

public class SamplePracticeTests {

    @Test(enabled = true)
    public void testArray() {
        Assert.assertEquals(true, true);
    }
}
